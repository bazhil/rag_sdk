# Автоматический кросс-языковой поиск

## Обзор

RAG теперь автоматически определяет языки документов и запросов, применяя интеллектуальные стратегии для улучшения качества поиска в кросс-языковых сценариях.

**Никаких настроек не требуется** - система работает полностью автоматически! 🚀

## Как это работает

### 1. При загрузке документа

```
Документ загружается
    ↓
Текст извлекается и разбивается на фрагменты
    ↓
🔍 Автоматическое определение языка документа
    ↓
Язык сохраняется в метаданных документа
    ↓
Создаются векторные представления
```

**Поддерживаемые языки:**
- Русский (ru)
- Английский (en)
- Немецкий (de)
- Французский (fr)
- Испанский (es)
- И 50+ других языков

### 2. При выполнении поиска

```
Пользователь вводит запрос
    ↓
🔍 Автоматическое определение языка запроса
    ↓
Получение языка целевого документа (если указан document_id)
    ↓
┌─────────────────────────────────────────┐
│ Языки совпадают?                        │
├─────────────────────────────────────────┤
│ ДА  → Обычный поиск                     │
│ НЕТ → Кросс-языковой режим             │
│       • Перевод запроса на язык документа│
│       • Поиск по обоим вариантам        │
│       • Объединение и ранжирование      │
└─────────────────────────────────────────┘
    ↓
Возвращаются лучшие результаты
```

## Примеры работы

### Пример 1: Английский документ + Русский запрос

```python
# Загружаем английский документ
doc_id = await rag.add_document("machine_learning.pdf", "ml.pdf")
# Система автоматически определяет: language = "en"

# Поиск на русском
results = await rag.search(
    query="что такое машинное обучение?",
    document_id=doc_id
)

# Что происходит внутри:
# 1. Определяется язык запроса: "ru"
# 2. Язык документа: "en"
# 3. Обнаружено несоответствие!
# 4. Запрос переводится: "what is machine learning?"
# 5. Поиск выполняется по обоим вариантам:
#    - Оригинал: "что такое машинное обучение?"
#    - Перевод: "what is machine learning?"
# 6. Результаты объединяются и сортируются
```

**Логи:**
```
[LANG_DETECTOR] Detected language: ru
[RAG_MANAGER] Document language: en
[RAG_MANAGER] Cross-lingual search detected (ru -> en)
[LANG_DETECTOR] Translated from ru to en
  Original: что такое машинное обучение?
  Translated: what is machine learning?
[RAG_MANAGER] Will search with 2 query variants
```

### Пример 2: Русский документ + Английский запрос

```python
# Загружаем русский документ
doc_id = await rag.add_document("руководство.pdf", "guide.pdf")
# Система автоматически определяет: language = "ru"

# Поиск на английском
results = await rag.search(
    query="how to configure the system?",
    document_id=doc_id
)

# Запрос автоматически переводится на русский:
# "как настроить систему?"
```

### Пример 3: Одинаковые языки

```python
# Английский документ
doc_id = await rag.add_document("guide.pdf", "guide.pdf")

# Английский запрос
results = await rag.search(
    query="how to install?",
    document_id=doc_id
)

# Перевод не выполняется - языки совпадают
# Обычный поиск с мультиязычной моделью
```

### Пример 4: Поиск по всем документам

```python
# Документы на разных языках
await rag.add_document("en_doc.pdf", "en.pdf")  # english
await rag.add_document("ru_doc.pdf", "ru.pdf")  # russian
await rag.add_document("de_doc.pdf", "de.pdf")  # deutsch

# Поиск на русском по всем документам
results = await rag.search(
    query="искусственный интеллект",
    document_id=None  # поиск по всем
)

# Мультиязычная модель embeddings находит релевантные результаты
# во всех документах благодаря единому векторному пространству
```

## Используемые технологии

### 1. Мультиязычная Embedding модель

**По умолчанию:** `paraphrase-multilingual-mpnet-base-v2`

Эта модель создает векторные представления текстов на разных языках в едином векторном пространстве, где семантически похожие тексты на разных языках располагаются близко друг к другу.

### 2. Автоматическое определение языка

**Библиотека:** `langdetect`

Быстрая и точная библиотека для определения языка текста:
- Поддержка 55+ языков
- Высокая точность (>99% для текстов >20 слов)
- Детерминированные результаты

### 3. Машинный перевод

**Библиотека:** `deep-translator` (Google Translate API)

Автоматический перевод запросов при обнаружении языкового несоответствия:
- Поддержка 100+ языков
- Высокое качество перевода
- Современная и стабильная библиотека
- Бесплатное использование

## Преимущества автоматического подхода

### ✅ Нет необходимости в настройке

Не нужно указывать:
- Язык документов
- Язык запросов
- Когда использовать перевод
- Какую стратегию применять

Все определяется автоматически!

### ✅ Работает с документами на разных языках

Можно загружать документы на любых языках:
- Английский документ
- Русский документ  
- Немецкий документ
- Все вместе

И делать запросы на любом языке - система сама разберется!

### ✅ Умная оптимизация

Перевод выполняется только когда это действительно нужно:
- Если языки совпадают → обычный поиск (быстро)
- Если языки различаются → поиск с переводом (точно)

### ✅ Комбинированный поиск

При кросс-языковом поиске выполняется:
1. Поиск с оригинальным запросом (мультиязычная модель)
2. Поиск с переведенным запросом (точное соответствие)
3. Объединение и ранжирование результатов

Это дает **максимальное качество** поиска!

## Мониторинг работы

### Просмотр определенных языков

Язык документа сохраняется в метаданных:

```python
documents = await rag.get_documents()

for doc in documents:
    lang = doc['metadata'].get('language', 'unknown')
    print(f"{doc['filename']}: {lang}")
```

### Проверка кросс-языкового поиска

В логах вы увидите:

```
[LANG_DETECTOR] Detected language: ru
[RAG_MANAGER] Document language: en
[RAG_MANAGER] Cross-lingual search detected (ru -> en)
[LANG_DETECTOR] Translated from ru to en
  Original: ваш запрос
  Translated: your query
```

## Требования

### Обязательные зависимости

Уже включены в `requirements.txt`:

```txt
sentence-transformers==3.3.0  # Мультиязычные embeddings
langdetect==1.0.9             # Определение языка
deep-translator==1.11.4       # Перевод
```

### Установка

```bash
pip install -r requirements.txt
```

Или для Docker:

```bash
docker compose up --build
```

Зависимости установятся автоматически!

## Решение проблем

### Перевод не работает

**Симптомы:**
```
[LANG_DETECTOR] WARNING: deep-translator not installed
```

**Решение:**
```bash
pip install deep-translator
```

### Определение языка не работает

**Симптомы:**
```
[LANG_DETECTOR] WARNING: langdetect not installed
```

**Решение:**
```bash
pip install langdetect
```

### Низкое качество поиска

**Проверьте:**
1. Используется ли мультиязычная модель:
   ```python
   # В RAG/config.py
   embedding_model: str = "sentence-transformers/paraphrase-multilingual-mpnet-base-v2"
   ```

2. Переиндексированы ли документы после смены модели

3. Работает ли перевод (см. логи)

### Неправильно определяется язык

**Причина:** Слишком короткий текст

**Решение:** Для коротких запросов (<10 слов) определение языка может быть неточным. Это нормально - мультиязычная модель все равно найдет релевантные результаты.

### Медленная работа

**Причина:** Перевод через интернет

**Решение:** 
- Перевод выполняется через Google Translate API
- Для production рассмотрите локальные модели перевода
- Или используйте только мультиязычную модель без перевода

## Примеры использования

### Базовое использование

```python
from RAG import RAGManager

async def main():
    rag = RAGManager()
    await rag.initialize()
    
    # Загружаем документ (язык определяется автоматически)
    doc_id = await rag.add_document("document.pdf", "doc.pdf")
    
    # Поиск на любом языке - все автоматически!
    results = await rag.search(
        query="your query in any language",
        document_id=doc_id
    )
    
    for result in results:
        print(f"Similarity: {result['similarity']:.2%}")
        print(f"Text: {result['content'][:200]}...")
    
    await rag.close()
```

### Проверка языка документа

```python
# Получить информацию о документе
doc = await rag.get_document(doc_id)
language = doc['metadata'].get('language')

print(f"Document language: {language}")
```

### Мультиязычная библиотека

```python
# Загружаем документы на разных языках
docs = {
    'en': await rag.add_document("en/intro.pdf", "intro_en.pdf"),
    'ru': await rag.add_document("ru/введение.pdf", "intro_ru.pdf"),
    'de': await rag.add_document("de/einführung.pdf", "intro_de.pdf"),
}

# Поиск на русском найдет релевантные фрагменты во всех документах
results = await rag.search(
    query="что такое искусственный интеллект?",
    document_id=None  # поиск по всем
)
```

## Сравнение с ручной настройкой

| Параметр | Ручная настройка | Автоматический режим |
|----------|------------------|----------------------|
| Настройка | Требуется указывать языки | ❌ Не требуется |
| Гибкость | Один язык документов | ✅ Любые языки |
| Точность | Зависит от настроек | ✅ Всегда оптимальная |
| Удобство | Нужно помнить настройки | ✅ Работает из коробки |
| Поддержка | Требуется для каждого языка | ✅ Автоматическая |

## Рекомендации

### Для максимального качества

1. **Используйте мультиязычную модель** (уже настроено по умолчанию)
2. **Убедитесь, что установлены все зависимости:**
   ```bash
   pip install langdetect googletrans==4.0.0rc1
   ```
3. **Переиндексируйте старые документы** после обновления

### Для быстрой работы

Если перевод замедляет работу, можно отключить `googletrans`:
```bash
pip uninstall googletrans
```

Поиск будет работать только с мультиязычной моделью (тоже хорошо!)

### Для production

Рассмотрите:
- Кэширование переводов в Redis
- Использование локальных моделей перевода
- Batch-обработку запросов

## FAQ

**Q: Нужно ли указывать язык документов?**  
A: Нет! Язык определяется автоматически при загрузке.

**Q: Нужно ли указывать язык запросов?**  
A: Нет! Язык определяется автоматически при каждом поиске.

**Q: Работает ли со старыми документами?**  
A: Для старых документов язык не определен. Переиндексируйте их.

**Q: Можно ли отключить автоматический перевод?**  
A: Да, просто удалите `deep-translator`. Мультиязычная модель продолжит работать.

**Q: Какие языки поддерживаются?**  
A: 55+ языков для определения, 100+ для перевода.

**Q: Требуется ли интернет?**  
A: Только для перевода (deep-translator). Определение языка работает офлайн.

## См. также

- [Установка и настройка](INSTALL.md)
- [Использование API](USAGE.md)
- [Настройка embedding моделей](ENV_SETUP.md)
- [Улучшение качества](IMPROVEMENTS.md)

