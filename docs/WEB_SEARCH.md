# 🌐 Веб-поиск в RAG

## Описание

Функция веб-поиска позволяет искать информацию в интернете с помощью DuckDuckGo и получать суммаризированные ответы с источниками.

## Как использовать

1. **Переключитесь на режим "Веб-поиск"** в боковой панели
2. **Введите запрос** в поле ввода
3. **Получите ответ** с суммаризацией и ссылками на источники

## Настройка

В файле `.env` можно настроить параметры веб-поиска:

```bash
# Количество результатов поиска (по умолчанию 5)
WEB_SEARCH_RESULTS_COUNT=5

# Количество повторных попыток при ошибке (по умолчанию 3)
WEB_SEARCH_MAX_RETRIES=3
```

## Известные ограничения

### ⚠️ Rate Limiting от DuckDuckGo

DuckDuckGo имеет строгие ограничения на автоматические запросы для предотвращения злоупотреблений. Это может привести к ошибкам типа `202 Ratelimit`.

**Симптомы:**
- Сообщение "К сожалению, не удалось найти результаты по вашему запросу"
- Ошибка `RatelimitException` в логах

**Решения:**

1. **Подождите 1-2 минуты** между запросами
2. **Используйте более простые запросы** вместо длинных или специфических
3. **Попробуйте запрос на английском языке** - он может работать лучше
4. **Перезапустите контейнер** если проблема сохраняется:
   ```bash
   docker compose restart app
   ```

### Альтернативные решения

Если веб-поиск не работает стабильно, рассмотрите альтернативные варианты:

1. **Режим RAG (Документы)**
   - Загрузите документы в систему
   - Используйте локальный поиск без ограничений

2. **API ключи для других поисковиков** (требует доработки)
   - Google Custom Search API
   - Bing Search API
   - SerpAPI

## Как работает веб-поиск

Система использует **двухуровневый подход** для максимальной надежности:

### Уровень 1: API метод (быстрый)
1. Пробует использовать библиотеку `duckduckgo-search` (2 попытки)
2. Если успешно - возвращает результаты

### Уровень 2: HTML fallback (надежный)
1. Если API заблокирован, переключается на прямые HTTP запросы
2. Имитирует реальный браузер (User-Agent, headers)
3. Парсит HTML версию DuckDuckGo (`html.duckduckgo.com`)
4. До 3 попыток с увеличивающимися задержками

### Финальная обработка
1. **Извлечение контента** - для каждого результата загружается содержимое веб-страницы
2. **Суммаризация LLM** - весь собранный контент отправляется в LLM для анализа
3. **Форматирование ответа** - LLM создает структурированный ответ с ссылками на источники

## Примеры запросов

### ✅ Хорошие запросы

- "What is RAG in AI?"
- "Python async programming"
- "Docker best practices"
- "machine learning basics"

### ⚠️ Проблемные запросы

- Слишком специфичные запросы на русском языке
- Очень длинные запросы с множеством параметров
- Запросы с специальными символами

## Отладка

Для просмотра логов веб-поиска:

```bash
docker compose logs app --tail=50 --follow
```

Ищите строки с префиксом `[WEB_SEARCH]` для диагностики проблем.

## Технические детали

### Используемые библиотеки

- `duckduckgo-search` - для поиска в DuckDuckGo
- `requests` - для HTTP запросов
- `beautifulsoup4` - для парсинга HTML
- LLM (Ollama/GigaChat/OpenAI) - для суммаризации

### Архитектура

```
User Query
    ↓
DuckDuckGo Search (5 results)
    ↓
Fetch Webpage Content (parallel)
    ↓
Combine all content
    ↓
LLM Summarization
    ↓
Formatted Response with Links
```

## Улучшения в будущем

- [ ] Добавить поддержку прокси для обхода rate limiting
- [ ] Интеграция с другими поисковиками
- [ ] Кэширование результатов поиска
- [ ] Параллельное извлечение контента страниц
- [ ] Фильтрация и ранжирование результатов
- [ ] Поддержка поиска изображений и видео

## FAQ

**Q: Почему веб-поиск не работает?**  
A: Скорее всего, DuckDuckGo заблокировал автоматические запросы. Подождите несколько минут и попробуйте снова.

**Q: Можно ли использовать другие поисковики?**  
A: В текущей версии - нет. Но вы можете доработать код и добавить поддержку других API.

**Q: Почему так мало результатов?**  
A: По умолчанию возвращается 5 результатов. Измените `WEB_SEARCH_RESULTS_COUNT` в `.env`.

**Q: Можно ли кэшировать результаты?**  
A: В текущей версии кэширование не реализовано, но это хорошая идея для будущего развития.

## Поддержка

Если у вас возникли проблемы с веб-поиском:

1. Проверьте логи: `docker compose logs app`
2. Убедитесь, что контейнер имеет доступ к интернету
3. Попробуйте простой запрос на английском языке
4. Подождите несколько минут если возникла ошибка rate limiting

---

**Примечание:** Веб-поиск - экспериментальная функция. Для стабильной работы рекомендуется использовать режим RAG с локальными документами.

