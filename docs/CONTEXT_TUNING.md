# Настройка размера контекста

## Проблема

По умолчанию в контекст помещается **7 фрагментов** документа для генерации ответа. Это значение можно настроить в зависимости от ваших потребностей.

## Как это работает

Когда вы задаете вопрос:
1. Система ищет наиболее релевантные фрагменты в документах
2. Берет топ-N фрагментов (где N = `SEARCH_LIMIT`)
3. Отправляет их в LLM в качестве контекста
4. LLM генерирует ответ на основе этих фрагментов

**Больше фрагментов = более полный контекст, но:**
- Дольше генерация ответа
- Больше токенов в промпте
- Может быть "шум" из менее релевантных фрагментов

**Меньше фрагментов = быстрее, но:**
- Может упустить важную информацию
- Менее полные ответы

## Настройка

### Способ 1: Через переменную окружения (рекомендуется)

В файле `.env`:

```env
# Количество фрагментов в контексте
SEARCH_LIMIT=7
```

**Рекомендуемые значения:**

| Значение | Использование | Описание |
|----------|---------------|-----------|
| 3-5 | Быстрые ответы | Для простых вопросов, требующих быстрого ответа |
| 7-10 | Стандартные ответы | **По умолчанию**, баланс скорости и качества |
| 10-15 | Развернутые ответы | Для сложных вопросов, требующих полного контекста |
| 15-20 | Максимальный контекст | Для очень сложных вопросов (медленно) |

### Способ 2: Программно через API

```python
from RAG import RAGManager

async def main():
    rag = RAGManager()
    await rag.initialize()
    
    # Задаем свое количество фрагментов
    result = await rag.generate_answer(
        query="Ваш вопрос",
        document_id=1,
        context_limit=10  # Используем 10 фрагментов
    )
    
    print(result['answer'])
    await rag.close()
```

### Способ 3: Через REST API

```bash
curl -X POST "http://localhost:8000/api/chat" \
  -H "Content-Type: application/json" \
  -d '{
    "query": "Ваш вопрос",
    "document_id": 1,
    "context_limit": 10
  }'
```

## Примеры настройки

### Для коротких быстрых ответов

```env
SEARCH_LIMIT=3
MIN_SIMILARITY=0.5
```

**Результат:**
- 3 самых релевантных фрагмента
- Высокий порог similarity (0.5)
- Быстрая генерация
- Краткие ответы

### Для развернутых ответов

```env
SEARCH_LIMIT=15
MIN_SIMILARITY=0.3
```

**Результат:**
- 15 фрагментов контекста
- Низкий порог similarity (0.3)
- Более медленная генерация
- Полные, развернутые ответы

### Баланс (рекомендуется)

```env
SEARCH_LIMIT=7
MIN_SIMILARITY=0.4
```

**Результат:**
- 7 фрагментов (оптимально)
- Средний порог similarity
- Хорошая скорость
- Достаточно полные ответы

## Проверка текущих настроек

### Через логи

При запуске приложения вы увидите:

```
RAG_SDK CONFIG - Settings loaded:
...
SEARCH_LIMIT: 7
MIN_SIMILARITY: 0.4
...
```

### Программно

```python
from RAG.config import settings

print(f"Search limit: {settings.search_limit}")
print(f"Min similarity: {settings.min_similarity}")
```

## Мониторинг использования контекста

### В логах сервера

При каждом запросе вы увидите:

```
[RAG_MANAGER] Context limit: 7
[RAG_MANAGER] Found 7 relevant chunks:
[RAG_MANAGER]   Chunk 1: filename.pdf (similarity: 85.2%, index: 5)
[RAG_MANAGER]   Chunk 2: filename.pdf (similarity: 82.1%, index: 3)
...
[RAG_MANAGER] Total context: 3542 chars, 687 words
```

Это покажет:
- Сколько фрагментов используется
- Их релевантность (similarity)
- Общий размер контекста

## Оптимизация

### Для больших документов

Если документ очень большой (>100 страниц):

```env
SEARCH_LIMIT=10
MIN_SIMILARITY=0.5
CHUNK_SIZE=800
```

- Больше фрагментов для полноты
- Выше порог для качества
- Больший размер чанка для контекста

### Для коротких документов

Если документы короткие (<10 страниц):

```env
SEARCH_LIMIT=5
MIN_SIMILARITY=0.3
CHUNK_SIZE=300
```

- Меньше фрагментов (их и так мало)
- Ниже порог (может быть мало совпадений)
- Меньший размер чанка

### Для кросс-языкового поиска

При использовании автоматического перевода:

```env
SEARCH_LIMIT=10
MIN_SIMILARITY=0.35
```

- Больше фрагментов (поиск по 2 вариантам запроса)
- Чуть ниже порог (учитываем погрешность перевода)

## Влияние на производительность

| Параметр | Влияние на скорость | Влияние на качество |
|----------|---------------------|---------------------|
| SEARCH_LIMIT=3 | ⚡⚡⚡ Очень быстро | ⭐⭐ Базовое |
| SEARCH_LIMIT=7 | ⚡⚡ Быстро | ⭐⭐⭐ Хорошее |
| SEARCH_LIMIT=10 | ⚡ Нормально | ⭐⭐⭐⭐ Отличное |
| SEARCH_LIMIT=15 | 🐌 Медленно | ⭐⭐⭐⭐⭐ Максимальное |

**Время генерации (примерно):**
- 3 фрагмента: ~2-3 сек
- 7 фрагментов: ~3-5 сек
- 10 фрагментов: ~5-7 сек
- 15 фрагментов: ~7-10 сек

*Зависит от LLM провайдера и размера фрагментов*

## Рекомендации

### Общие

1. **Начните с 7** - это оптимальный баланс
2. **Увеличивайте постепенно** - по 2-3 фрагмента
3. **Следите за качеством** - проверяйте ответы
4. **Учитывайте скорость** - важна ли вам скорость ответа?

### По типу задач

**FAQ и простые вопросы:**
```env
SEARCH_LIMIT=3
MIN_SIMILARITY=0.6
```

**Техническая документация:**
```env
SEARCH_LIMIT=10
MIN_SIMILARITY=0.4
```

**Исследовательские вопросы:**
```env
SEARCH_LIMIT=15
MIN_SIMILARITY=0.3
```

**Чат-бот с быстрыми ответами:**
```env
SEARCH_LIMIT=5
MIN_SIMILARITY=0.5
```

## Устранение проблем

### Ответы слишком короткие

**Проблема:** LLM дает неполные ответы

**Решение:**
```env
SEARCH_LIMIT=10  # Увеличить контекст
MIN_SIMILARITY=0.35  # Понизить порог
```

### Ответы слишком медленные

**Проблема:** Генерация занимает много времени

**Решение:**
```env
SEARCH_LIMIT=5  # Уменьшить контекст
MIN_SIMILARITY=0.5  # Повысить порог (меньше фрагментов)
```

### Ответы не по теме

**Проблема:** LLM использует нерелевантную информацию

**Решение:**
```env
SEARCH_LIMIT=5  # Меньше фрагментов
MIN_SIMILARITY=0.6  # Строже фильтрация
```

### "Не найдено релевантной информации"

**Проблема:** Система не находит подходящих фрагментов

**Решение:**
```env
MIN_SIMILARITY=0.3  # Понизить порог
SEARCH_LIMIT=10  # Увеличить количество
```

## См. также

- [Настройка окружения](ENV_SETUP.md)
- [Улучшение качества](IMPROVEMENTS.md)
- [Кросс-языковой поиск](AUTO_MULTILINGUAL.md)

